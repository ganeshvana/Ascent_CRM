<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="compute_actuals" model="ir.cron">
        <field name="name">Compute Actuals</field>
        <field name="model_id" ref="model_crm_target"/>
        <field name="state">code</field>
        <field name="code">model.action_compute()</field>
        <field name="user_id" ref="base.user_root" />
        <!-- <field name="interval_number"></field> -->
        <!-- <field name="interval_type"></field> -->
        <field name="numbercall">-1</field>
        <!-- <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 12:00:00')"/> -->
        <field eval="False" name="doall" />
    </record>
    </data>


</odoo>
